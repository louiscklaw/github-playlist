
build-dashboard-tryout:
  runs-on: ubuntu-latest

  steps:
  - uses: actions/checkout@v2

  - run: |
      cd build-dashboard-tryout
        yarn
        yarn build
        # deploy handled by cron job on update-dashboard.yml
        # yarn deploy
      cd ..
