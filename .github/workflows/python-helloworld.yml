name: python-helloworld-tryout
# This workflow is triggered on pushes to the repository.
on: [push]

jobs:
  build:
    # Job name is Greeting
    name: Greeting
    # This job runs on Linux
    runs-on: ubuntu-latest
    steps:
      - name: python-helloworld-tryout
        run: python python-helloworld-tryout/helloworld.py
